.posts-wrapper.row
  .post
    .post-head
      .name
        Eugene Shved
    .image.center-block
      =image_tag @post.image.url(:medium)
    %p.caption
      =@post.caption
    .text-center
      =link_to "Return back", posts_path
      |
      =link_to "Edit Post", edit_post_path(@post)
